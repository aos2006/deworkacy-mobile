export { default as Locations } from './components';
import * as types from './actionTypes';
import * as actions from './actions';
import reducer from './reducer';

export default { types, actions, reducer };
